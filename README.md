# techflash 11.04.2019

First clone the repository (git clone https://github.com/cierpialaizabela/techflash)

Then download all required dependencies (npm install)

Finally, execute the tests (npm test)
